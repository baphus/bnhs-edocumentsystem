[phases.setup]
nixPkgs = ["nodejs_23", "php84", "php84Packages.composer"]

[phases.install]
cmds = [
    "composer install --optimize-autoloader --no-dev --no-interaction --no-scripts",
    "npm install"
]

[phases.build]
cmds = [
    "npm run build"
]

[variables]
CACHE_STORE = "file"

[start]
cmd = "bash railway-start.sh"
